# Archived Desktop UI (PySide6)

This folder stores the legacy desktop UI implementation that was previously mixed
inside the backend project.

Status:

- Archived only
- Not part of the active Web flow
- Not used by default startup scripts

Primary stack in this archive:

- Python + PySide6 desktop UI
- Legacy desktop startup scripts
- Legacy desktop packaging scripts

If you need to inspect old desktop behavior, review the archived files here.
Active development should stay on:

- `fileclassifier-python-api` (API + services)
- `fileclassifier-react-ui` (frontend UI)
