# 附：核实清单

> 更新记录：2026-08-03 二次核实后修正 2 项（storage 死引用实为文档过时；/api 双前缀已确认）。
- [x] 仓库类型：Python(FastAPI)+React 全栈、文件归档/匹配领域、monorepo、生产级（有测试/部署文档/桌面打包）。依据：`README.md`、`pyproject.toml`、`package.json`、`DEPLOYMENT.md`。
- [x] 运行时依赖仅 pandas/openpyxl/fastapi/uvicorn/python-multipart（无 DB/向量/LLM）。依据：`pyproject.toml`。
- [x] 列画像逻辑：`analyze_columns` + `_profile_column` + `_looks_like_range`。依据：`file_matcher.py:38-91,184-279`。
- [x] 文件名 token 匹配 + context 回显：`get_identifier_tokens` + `match_files`。依据：`file_matcher.py:99-182`。
- [x] 条件 DSL 4 算子 + range 二分索引：`QueryEngine` 全文件。依据：`query_engine.py`。
- [x] 工作流重算匹配 + 复制 + 报告 + 日志：`WorkflowService.run` / `_copy_file` / `write_report` / `append_log`。依据：`workflow.py`、`system_io.py`。
- [x] 运行时目录双模抽象：`get_runtime_dirs` + `FILECLASSIFIER_DESKTOP_MODE` / `runtime_dir`。依据：`pathing.py`、`start_desktop.py`。
- [x] 前端三步向导 + `/api` 前缀 + 无组件库/状态管理。依据：`App.jsx`、`src/lib/api.js`、`package.json`、`components/`。
- [x] 测试覆盖核心 service 层，夹具含边界情形。依据：`tests/conftest.py`、`test_file_matcher.py`、`mock_data.py`。
- [x] 三个启动入口端口不一致（8000 / 8000 / 18080）。依据：`pyproject.toml`(main 写死 8000)、`start_web.py`(8000)、`start_desktop.py`(18080)。

## ⚠️ 待核实（无法从已读代码确认，或需进一步动作）
- [x] **`storage.py` 死引用（已澄清，非代码问题）**：在 `src/fileclassifier/` 全量搜索 `storage` 结果为 **0 处引用**，源码并不存在对 `storage.py` 的 import。原架构图里写的 `storage.py` 是 **README/文档层面过时的描述**，实际代码已无此模块。结论修正为「文档与代码不一致」类坑（见 4.3 坑 2 修正）。
- [x] **前端 `/api` 双前缀（已确认真实存在）**：`src/lib/api.js:1` 基址 `VITE_API_BASE || "/api"`，而后端路由前缀本身已含 `/api`（`/api/excel` 等）。`vite.config.js:9-14` 确有 proxy：`"/api" → http://127.0.0.1:8000`。经 dev proxy 后实际请求路径会变成 `/api/api/excel`，**双 `/api` 配置契约瑕疵确认成立**（见 4.3 坑 3）。生产部署需反代端消除一层 `/api`。
- [ ] **路径穿越防护**：`workflow._copy_file` 拼目标路径时是否有 `is_relative_to` / `../` 校验，当前读取未覆盖该细节。→ 需再读 `workflow.py:97-150` 的细节。
- [ ] **桌面模式实际打包产物是否包含 `mock_data`/tests**：`build-windows/` 含 `.pyz` 等，未逆向打包脚本内容。→ 非核心，低优先。
- [ ] **`legacy/` 与当前代码关系**：早期 SVG/ps1 原型，README 未说明是否已完全弃用。→ 仅作背景，不影响主分析。

## 核实方法说明
本分析采用提示词「方式 A」：直接在 CodeBuddy 中读取本地仓库源码，所有带文件:行号的结论均来自实际读取。未使用方式 B（DeepWiki 等外部分析器）。如需交叉验证，可把本仓库（`fileclassifier`）交给 DeepWiki 生成 wiki 后与本文档对比，重点核实上述"待核实"项。
